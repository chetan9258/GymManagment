import { ConfirmEqualValidator.Directive.TsDirective } from './confirm-equal-validator.directive.ts.directive';

describe('ConfirmEqualValidator.Directive.TsDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmEqualValidator.Directive.TsDirective();
    expect(directive).toBeTruthy();
  });
});
